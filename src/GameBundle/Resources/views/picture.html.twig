{% extends 'SonataAdminBundle:CRUD:base_list_field.html.twig' %}
{% block field %}
    {% if object.url|slice(0, 2) == '//' %}
        <img src="http:{{ object.url }}">
    {% else %}
        <img src="{{ object.url }}">
    {% endif %}
{% endblock %}